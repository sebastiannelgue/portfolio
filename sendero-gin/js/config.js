/* ============================================================
   SENDERO GIN — config.js
   ------------------------------------------------------------
   TESTING: true  → bypass FormSubmit (no se envían mails)
   TESTING: false → producción, se envían los mails reales
   ============================================================ */
window.TESTING = false;
